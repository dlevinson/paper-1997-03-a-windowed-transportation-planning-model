t  functions  inita fd1  =(60 * length / 55) * (1 + put(volau / (2000 * lanes)) + put(get(        1) * get(1)) * get(2))a fd2  =(60 * length / 45) * (1 + .255 * exp(put(volau / (1600 * lanes))        ) + .3 * put(get(1) * get(1)) * get(2))a fd3  =(60 * length / 35) * (1 + .188 * exp(put(volau / (800 * lanes)))         + .48 * put(get(1) * get(1)) * get(2))a fd4  =(60 * length / 10) * (1 + .111 * exp(put(volau / (400 * lanes)))         + .68 * put(get(1) * get(1)) * get(2))a fd8  =(60 * length / 45) * (1 + put(volau / (1200 * lanes)) + put(get(        1) * get(1)) * get(2))a fd10 =.01a fd21 =(60 * length / 25) * (1 + .255 * exp(put(volau / (700 * lanes)))         + .3 * put(get(1) * get(1)) * get(2))a fd22 =(60 * length / 35) * (1 + .255 * exp(put(volau / (1600 * lanes))        ) + .3 * put(get(1) * get(1)) * get(2))a fp1  =(30 * (put(volau / (2000 * up1 * up2)) - 1)) * (get(1) .gt. 1) *         (up1 .gt. .00001) * (up1 .le. 1) * (up2 .gt. .00001) * (up2             .le. 9) + ((up3 / 60) * (1 - up1) / 2) * (up1 .gt. .00001) * (          up1 .le. 1) * (up3 .gt. .00001) * (up3 .le. 120)a fp2  =(30 * (put(volau / (2000 * up1 * up2)) - 1)) * (get(1) .gt. 1) *         (up1 .gt. .00001) * (up1 .le. 1) * (up2 .gt. .00001) * (up2             .le. 9) + ((up3 / 60) * (1 - up1) / 2) * (up1 .gt. .00001) * (          up1 .le. 1) * (up3 .gt. .00001) * (up3 .le. 120)          